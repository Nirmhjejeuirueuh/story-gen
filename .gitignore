node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Runtime file-based "database" — regenerated on startup. Kept out of git because it holds
# API keys (settings.geminiApiKey) and bloats the repo with inlined base64 images.
data_db.json
